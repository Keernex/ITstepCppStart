#include <iostream>
using std::endl;
using std::cout;
using std::cin;
int main()
{
    double q;
    double w;
    double e;
    cin >> q;
    cin >> w;
    cin >> e;

    cout << q * w + (e * w * 2) / 2;
}